/**
 * @author torith
 * @date 2018-11-13
 */

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){

    cin.tie(0);
    ios::sync_with_stdio(false);
    int N; cin >> N;
    int odd_sum = 0, even_sum = 0;
    int a[50];
    for (int i = 0; i < N; ++i) {
        cin >> a[i];
    }

    return 0;
}

