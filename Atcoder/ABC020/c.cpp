/**
 * @author torith
 * @date 2018-11-21
 */

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

struct edge {
    int to;
    int white;
    int black;
};

vector<edge> G[100];

int main(){

    cin.tie(0);
    ios::sync_with_stdio(false);

    int H, W, T; cin >> H >> W >> T;
    int start, goal;

    for (int i = 0; i < H; ++i) {
        for (int j = 0; j < W; ++j) {
            char t; cin >> t;
            if (t == '.')
        }
    }



    return 0;
}

