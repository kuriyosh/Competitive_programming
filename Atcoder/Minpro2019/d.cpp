/**
 * @author torith
 * @date 2019-02-09
 */

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){

    cin.tie(0);
    ios::sync_with_stdio(false);

    int L; cin >> L;
    vector<int> A;
    for (int i = 0; i < L; ++i) {
        int t; cin >> t;
        A.push_back(t);
    }

    if()

    return 0;
}

