use proconio::input;
use proconio::marker::Chars;

fn main() {
    input! {
        ha: usize,
        wa: usize,
        a: [Chars; ha],
        hb: usize,
        wb: usize,
        b: [Chars; hb],
        hx: usize,
        wx: usize,
        x: [Chars; hx],
    }
}
